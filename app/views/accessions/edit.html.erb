<h4><%= MLOG_VOCAB["partner_codes"][@collection.partner_code] %> </h4>

<h4><%= @collection.collection_code  + ": " + @collection.title %></h4>

<%= form_for :accession, url: accession_path(@accession), method: :patch do |f| %>

  <p>
    <%= f.label :accession_num %><br>
    <%= f.text_field :accession_num %>
  </p>

  <p>
  	<%= f.label :accession_note %><br>
  	<%= f.text_area :accession_note %>
  <p>

  <p>
    <%= f.label :accession_state %><br>
    <%= f.select(:accession_state, MLOG_VOCAB["accession_state"].invert, :include_blank => false ) %></td>
  <p>

  <%= f.hidden_field :collection_id, :value => @collection.id %>


<p>
  <%= f.submit %>
</p>

<% end %>

<%= link_to 'Back', collections_path %>