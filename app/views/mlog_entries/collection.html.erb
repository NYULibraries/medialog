<h4><%= MLOG_VOCAB["partner_codes"][@mlog_entries[0].partner_code] %></h2>
<div class="lead"><%= MLOG_VOCAB["collection_codes"][@mlog_entries[0].collection_code] %></div>

<%= link_to "inventory view", "/collection/" + @mlog_entries[0].collection_code + "/uuids" %>
<br>
<b>accessions</b>:
<div class="well"> 
<% @accessions.each do |acc| %>
	<i><%= link_to acc, ("/accession?accession=" + acc + "&collection=" + @mlog_entries[0].collection_code) %></i>
<% end %>
</div>

<b>media objects</b>
<%= render 'btable' %>
